import style from './landing.module.css'

export const Back = ({height}) => {
    
    return (
            <div className={style.back}
            style={{height: height}}>
            </div>
    )
}

